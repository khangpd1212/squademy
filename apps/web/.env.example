# NestJS API base URL for server-side calls + rewrites proxy (/api/*)
# On Vercel set this to: http://161.118.198.102:3001/api
API_URL=http://localhost:4001/api

# NOTE: browser calls go through the same-origin /api/* rewrites (proxy).
# NEXT_PUBLIC_API_URL is no longer used — do not set it.
