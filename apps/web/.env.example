# NestJS API base URL for server-side calls (Server Components, Route Handlers)
API_URL=http://localhost:4001/api

# NestJS API base URL exposed to the browser (client components)
NEXT_PUBLIC_API_URL=http://localhost:4001/api
